{
  // 发布构建配置（07 篇 §8.3 思路照搬）：extends 主 tsconfig——typecheck
  // （tsc --noEmit）仍走主配置（测试文件类型覆盖不丢）；仅 build 链
  // （npm run build）改用本配置，产出零测试/声明/映射产物的干净 dist/：
  //   - exclude 覆盖父值（extends 数组不合并），故新增 src/**/*.test.ts
  //     （测试不入发布物）；
  //   - declaration/sourceMap 关：包只作 CLI 装机物（bin 单入口、无 exports 面），
  //     .d.ts/.js.map 是死重。
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": false,
    "sourceMap": false,
    "rootDir": "src"
  },
  // exclude 覆盖父值不合并（extends 语义）：本列表 = 父 exclude 重列 + 新增
  // 位——*.test.tsx（client 轨测试）、src/webui/client（SPA 树——vite 独立
  // 产物链 build:webui，tsc 发布物不含）与 tools/golden-record.ts（金样
  // 录制器——父 include 点名入列换类型检查，发布物侧在此出局）
  "exclude": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/webui/client", "tools/golden-record.ts"]
}
